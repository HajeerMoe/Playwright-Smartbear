Playwright Framework Part 1

1. Create a new folder and name it properly
    playwright-smartbear

2. Start an NPM project with playwright
    npm init playwright@latest
        
        -Select default details

3. Install dotenv dependency for setting up environment variables
    -npm i dotenv -D

    -create .env file
    -store your key
    -go to config file and uncomment the dotenv script lines
    -go to .env file and create baseURL = "paste url here"
    -go to config file and uncomment baseURL and put process.env.baseURL


4. In config, rename "projects" section to have more meaningful names
    -No one in real work names it "chromium", "firefox", etc
